<turbo-frame id="dashboard_content">
  <div class="flex justify-between items-center mb-6">
    <!-- Flash container -->
    <div class="fixed left-0 top-5 w-full flex justify-center items-center">
      <div id="dashboard_flash" class="bg-green-100 text-green-800 border-green-400 rounded-full">
        <%= render "shared/flash_messages" %>
      </div>
    </div>
    
    <h1 class="text-2xl font-bold">Destinations</h1>

    <%= link_to "Add Destination",
    new_admin_destination_path,
    data: { turbo_frame: "dashboard_content" },
    class: "bg-black text-white px-4 py-2 rounded hover:bg-gray-800" %>
  </div>

  <div id="destinations" class="grid grid-cols-3 gap-6">
    <% if @destinations.present? %>
    <%= render @destinations %>
    <% else %>
    <p>No Destinations available</p>
    <% end %>
  </div>
</turbo-frame>
<div  class="flex justify-between">
  <div>
    <p>Start Your Adventure</p>
    <p>Here’s Our Recommended Travel Destinations</p>
  </div>
  <span></span>
</div>

<%# <% Destination card %>
<div id="destinations">
  <% if @destinations.present? %>
    <%= render @destinations %>
  <% else %>
    No Destinations available
  <% end %>
</div>

<div class="px-20 py-10">

  <div class="mb-8">
    <p class="text-2xl font-semibold">Start Your Adventure</p>
    <p class="text-gray-500">Here’s Our Recommended Travel Destinations</p>
  </div>

  <div id="destinations" class="grid grid-cols-3 gap-6">
    <% if @destinations.present? %>
      <%= render @destinations %>
    <% else %>
      <p>No Destinations available</p>
    <% end %>
  </div>

</div>
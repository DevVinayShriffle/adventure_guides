<div class="bg-green-100 text-green-800 border-green-400 rounded-full">
<% flash.each do |type, message| %>
<div class="flash my-1 mx-4 flash-<%= type %>" data-controller="flash">
  <%= message %>
  <button type="button" data-action="flash#dismiss">
    &times;
  </button>
</div>
<% end %>
</div>